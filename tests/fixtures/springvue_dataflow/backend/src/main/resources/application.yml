spring:
  profiles:
    active: dev
  config:
    import: optional:nacos:order-service.yml
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
mybatis:
  mapper-locations: classpath:mapper/*.xml
